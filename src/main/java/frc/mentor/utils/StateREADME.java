package frc.mentor.utils;

/**
 * API exposée aux jeunes (state machines)
 *
 * - {@code interface StateMachine<I, O> { O update(I in, double dt); void reset(); }}
 * - String StateMachine.getStateName()
 * - boolean StateMachine.isFinished()
 * - int StateMachine.getStateId()
 * - void StateMachine.forceState(int id) (optionnel debug)
 * - void StateMachine.log(String key) (logs état courant)
 */
public final class StateREADME {
  private StateREADME() {}
}
