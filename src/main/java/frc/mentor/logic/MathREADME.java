package frc.mentor.logic;

/**
 * - Outils math génériques (clamp, deadband, wrapAngle, lerp, accel
 * trapezoidales).
 * - Helpers géométrie/units si besoin (sans hardware).
 */


/**
 * API exposée aux jeunes (math helpers)
 *
 * - Math2.clamp(double x, double lo, double hi)
 * - Math2.deadband(double x, double db)
 * - Math2.wrapAngleDeg(double deg)
 * - Math2.wrapAngleRad(double rad)
 * - Math2.lerp(double a, double b, double t)
 * - Math2.dist(double x0, double y0, double x1, double y1)
 */
public final class MathREADME {
    private MathREADME() {
    }
}
